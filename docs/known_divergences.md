# Known divergences

Source disagreements found while implementing mechanics, with the resolution. Log an entry whenever PS, pkmn/engine, Bulbapedia, or the cartridge disagree and you have to pick a side — so the call isn't re-litigated later.

Rule of thumb: **match PS** where PS disagrees with the cartridge (mimikyu's corpus is PS replays; cartridge-faithful behavior is a non-goal). Champions-specific data deltas belong in `docs/champions-data-deltas.md`; gen-9 interaction edges in `docs/interactions.md`. This file is for genuine source-vs-source disagreements.

Format per entry: **mechanic** — the disagreement (PS `file:line` + Bulbapedia URL) — the resolution.

_No entries yet._
